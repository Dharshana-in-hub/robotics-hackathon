# turtlebot3_waypoint_nav

This directory is a placeholder for the existing `turtlebot3_waypoint_nav`
ament_python package (containing `waypoint_loop_node.py`), which was
originally built/targeted for ROS 2 Humble.

**To do when you get here:**
1. Copy the actual package contents from your `~/turtlebot3_ws/src/` (or
   wherever it currently lives) into this folder, replacing this file.
2. Check `package.xml` and `setup.py` for any Humble-specific dependency pins
   and update them for Jazzy where needed.
3. Re-run `colcon build --symlink-install` from the workspace root and
   confirm the node still builds/launches cleanly under Jazzy.
4. Delete this placeholder file once real content is in place.
